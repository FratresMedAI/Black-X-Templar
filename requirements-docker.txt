# CPU-only stack for Gray Swan / Black Templar container (no GPU, no bitsandbytes).
# Judge profile should be no-llm-judge; semantic layer uses MiniLM on CPU.
requests==2.33.0
numpy==2.3.3
scipy==1.16.2
psutil==7.1.0
python-dotenv==1.1.1
fastapi
uvicorn[standard]
httpx==0.28.1
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4.0,<2.7.0
sentence-transformers==3.4.1
transformers>=4.51.3
accelerate>=1.3.0
